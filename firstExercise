 fct main() => {
   define proposition: string
   define wordsInProposition: string[]

   define 1test: string;
   
   read(proposition, keyboard)



   print("tes")
   wordsInProposition = split(proposition, ` `)

   return 'wordsInProposition
 }